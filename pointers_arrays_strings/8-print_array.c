#include "main.h"
#include <stdio.h>

/**
 * print_array - function that print n elements of an array of int
 * followed by new line.
 *
 *@a: array
 *@n: n elements
 */

void print_array(int *a, int n)
{
	int i,

	for (i = 0, i < n, i++)
	{
		printf("%i", a[i];
	}

}
